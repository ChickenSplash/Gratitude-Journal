{
  "name": "gratitude-journal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Single-page gratitude journal with guest mode and SQLite-backed user accounts",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.10.0",
    "express": "^4.21.2"
  }
}
